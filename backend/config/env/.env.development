# Development Environment Configuration

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=patient_info_dev
DB_USER=admin
DB_PASSWORD=Data123
DB_LOGGING=true

# JWT Configuration
JWT_SECRET=dev_jwt_secret_replace_in_production_environment
JWT_EXPIRES_IN=1h

# Flask Configuration (if applicable)
SECRET_KEY=dev_flask_secret_replace_in_production
JWT_SECRET_KEY=dev_flask_jwt_secret_replace_in_production
DATABASE_URL=sqlite:///app.db

# JWT Token Expiration
JWT_ACCESS_TOKEN_EXPIRES_HOURS=1
JWT_REFRESH_TOKEN_EXPIRES_DAYS=30

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS and Security Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Logging Configuration
LOG_LEVEL=debug
LOG_TO_FILE=false
LOG_FILENAME=logs/app-dev.log

# Cache Settings
CACHE_TTL=300